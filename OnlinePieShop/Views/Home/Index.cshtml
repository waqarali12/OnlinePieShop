@model HomeViewModel

@await Html.PartialAsync("_Carousel")

<h2>Pies of the week</h2>
<h4>Our weekly selection - just for you!</h4>
<div class="row">
    @foreach(var pie in Model.PiesOfTheWeek)
    {
        @Html.Partial("_PieOfTheWeekSummary",pie)

    }
</div>